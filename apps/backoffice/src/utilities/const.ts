export const placeholderImage = 'https://via.placeholder.com/300x300';

export enum StatusType {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    ARCHIVED = 'archived',
    DELETED = 'deleted',
}

export enum OperationType {
    CREATE = 'create',
    UPDATE = 'update',
}

{
    /*

              <Modal
                className="modal"
                isOpen={isOpen}
                setIsOpen={setIsOpen}
                testid="modal:brand-create"
                variant="center"
                onClose={(modal: any) => {}}
                width={565}
                withCloseIcon={false}
                renderContent={() => <CreateBrand loading={loading} setIsOpen={setIsOpen} />}
            />
            
            
            */
}
